[pytest]
testpaths = tests
markers =
    smoke: short end-to-end checks required before packaging
    integration: checks spanning multiple scientific pipeline modules
    dataset: checks requiring external or downloaded datasets
